# Todo List

These are the most important things that I should do:

1. Introduce a ratelimited screen (finish ratelimit).
2. Add `not-found`, `error` and `loading` pages.
3. Add `@nordaun/theme` and `@nordaun/color`.
4. Add `shadcn` for styling.
